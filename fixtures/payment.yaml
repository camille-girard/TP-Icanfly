App\Entity\Payment:
    payment1:
        totalPrice: 15000
        paymentDate: <(new \DateTimeImmutable())>
        status: <(App\Enum\PaymentStatus::from('confirmée'))>
        paymentType: <(App\Enum\PaymentType::from('card'))> 
        Mission: '@scientific_mission2'
    
    payment2:
        totalPrice: 10000
        paymentDate: <(new \DateTimeImmutable())>
        status: <(App\Enum\PaymentStatus::from('en attente'))>
        paymentType: <(App\Enum\PaymentType::from('stripe'))>
        Mission: '@scientific_mission1'
    
    payment3:
        totalPrice: 20000
        paymentDate: <(new \DateTimeImmutable())>
        status: <(App\Enum\PaymentStatus::from('supprimée'))>
        paymentType: <(App\Enum\PaymentType::from('crypto'))>
        Mission: '@scientific_mission3'
