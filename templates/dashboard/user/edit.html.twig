{% extends 'dashboard.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block content %}
    <div class="px-6 py-8 bg-[#F5F7FA] min-h-screen">
        <h1 class="text-3xl font-bold text-[#1E2A38] mb-8">Modifier l'utilisateur</h1>

        <div class="bg-white rounded-lg shadow-md p-6 max-w-3xl">
            {{ include('dashboard/user/_form.html.twig', {'button_label': 'Enregister'}) }}
        </div>
        <div class="flex justify-between mt-6">
            {{ include('dashboard/user/_delete_form.html.twig') }}
        </div>
        <div class="mt-6">
            <a href="{{ path('app_user_index') }}"  class="text-[#002EFF] hover:underline">&larr; Retour à la liste</a>
        </div>
    </div>
{% endblock %}
