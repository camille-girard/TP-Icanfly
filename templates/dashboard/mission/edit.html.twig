{% extends 'dashboard.html.twig' %}

{% block title %}Edit Mission{% endblock %}

{% block content %}
    <h1>Edit Mission</h1>

    {{ include('dashboard/mission/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('dashboard_mission_index') }}">back to list</a>

    {{ include('dashboard/mission/_delete_form.html.twig') }}
{% endblock %}
