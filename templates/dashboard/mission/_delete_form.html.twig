<form method="post" action="{{ path('dashboard_mission_delete', {'id': mission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
    <button class="btn bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded">Delete</button>
</form>
